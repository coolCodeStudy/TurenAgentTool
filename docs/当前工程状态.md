# 当前工程状态

## 当前定位

InvestmentKnowledge MCP 当前是一个投资知识图谱系统的第一版后端骨架。它的重点不是自动交易，而是把个股研究、板块归类、事实知识、用户心得和后续复盘沉淀成可查询、可确认、可追溯的知识库。

个人 Mac 是开发环境和交互入口，不是长期运行环境。长期目标是部署到阿里云 ECS 或其他云端环境。

## 已完成

### 需求和设计

- 原始需求已整理为 `我的需求.md`。
- 技术方案、数据模型、MCP 工具设计和 MVP 路线图已持久化在 `docs/`。
- PostgreSQL schema 已放在 `db/schema.sql`。

### 数据库和 MCP

- 使用 PostgreSQL + pgvector。
- 本地开发数据库通过 Docker Compose 启动。
- 宿主机端口为 `55432`，避免与 ReportSystem 的 PostgreSQL `5432` 冲突。
- MCP Server 使用 Python + FastMCP。
- 本地默认 `stdio` transport，云端使用 `streamable-http`。

### 已实现 MCP 工具

- `search_stock`
- `get_stock_context`
- `get_sector_context`
- `upsert_stock_profile`
- `upsert_sector_tree`
- `link_stock_to_sector`
- `add_source`
- `add_knowledge_item`
- `add_user_insight`
- `record_user_insight`
- `import_stock_research_draft`

### 个股研究链路

当前链路已经可以完整跑通：

```text
资料来源
  -> 生成研究草稿骨架
  -> 生成模型 prompt
  -> 模型补全 enriched draft
  -> 校验 JSON 和来源引用
  -> 生成人工审阅稿
  -> 用户确认
  -> confirmed 入库
```

已支持：

- 手工资料来源 provider。
- 公开网页抓取 provider。
- OpenAI Responses API provider。
- mock provider，用于不花钱跑通流程。
- 草稿校验器。
- 人工审阅稿生成器。

### 分析上下文与用户记忆

已实现第一版分析上下文层：

```text
个股/板块
  -> 股票画像和板块路径
  -> 事实知识和来源
  -> 个股心得
  -> 板块心得
  -> portfolio / strategy 级用户偏好
  -> Markdown 分析上下文
```

已支持：

- `get_stock_context`：构建个股分析上下文。
- `get_sector_context`：构建板块分析上下文。
- `record_user_insight`：按股票代码、板块路径或全局类型写入用户心得。
- `scripts/build_analysis_context.py`：导出可读 Markdown context。

### 部署准备

- 已新增 `Dockerfile`。
- 已新增 `docker-compose.prod.yml`。
- 已新增 `.env.prod.example`。
- 已新增 `DEPLOYMENT.md`。
- 已新增 systemd service 模板。
- 已本地验证 production compose 可以启动 MCP + pgvector。

## 当前没有做

- 尚未真正部署到阿里云。
- 尚未接 Futu MCP。
- 尚未接同花顺 MCP。
- 尚未实现每日持仓复盘。
- 尚未实现用户长期偏好画像。
- 尚未实现雪球 cookie provider。
- 尚未做前端或 Web 管理界面。

## 当前推荐下一步

优先顺序建议：

1. 用分析上下文回答一次真实的个股/板块分析问题。
2. 将用户对分析结果的反馈沉淀为 `stock`、`sector`、`portfolio` 或 `strategy` 心得。
3. 再进入持仓复盘或云端部署。

## 验证命令

```bash
python scripts/smoke_test.py
python scripts/validate_research_draft.py examples/sk_hynix_research_draft.json
python scripts/review_research_draft.py examples/sk_hynix_research_draft.json
python scripts/build_analysis_context.py 000660 KR --output drafts/000660_KR_analysis_context.md
```
