# 2026-05-19 核心记录

## 当前状态

- 项目仍处于云端 MVP 验证阶段。
- 本地 MVP 和阿里云 ECS 部署链路此前已跑通。
- ECS 上 `postgres` 和 `dingtalk-stream-bot` 容器此前已能启动。
- 当前卡点仍是云端钉钉群里 `@接接接的claw` 暂时没有响应。

## 今日完成

- 自动保存了当天核心记录。
- 未观察到新的代码提交或新的排查结果。

## 当前卡点

- 需要确认 DingTalk Stream bot 在 ECS 上是否真正连上钉钉长连接。
- 需要确认钉钉是否把群消息投递给 bot。
- 需要通过容器日志判断是否有 OpenAI、数据库或命令解析错误。

## 下一步

继续优先查看 ECS 容器日志：

```bash
cd /opt/investment-knowledge
docker compose -f docker-compose.prod.yml ps
docker compose -f docker-compose.prod.yml logs --tail=200 dingtalk-stream-bot
docker compose -f docker-compose.prod.yml logs --tail=80 postgres
```

随后在钉钉群里测试：

```text
@接接接的claw 帮助
@接接接的claw 怎么看海力士
```

