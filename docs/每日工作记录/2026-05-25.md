# 2026-05-25 核心记录

## 当前状态

- ECS 上 Futu OpenD 已启动，钉钉 `我的持仓` 指令已能触发云端持仓查询链路。
- 当前错误来自 OpenD 安全限制：直接从容器访问宿主机 `0.0.0.0:11111` 会被判定为跨网交易连接。
- 代码侧已改为默认通过 `host.docker.internal:11112` 访问宿主机代理，OpenD 应只监听 ECS 本机 `127.0.0.1:11111`。

## 今日完成

- 确认 OpenD 进程存在，且 `11111` 端口监听成功。
- 钉钉侧触发 `我的持仓`，错误从连接失败推进到 `position_list_query()` 参数不兼容。
- 修复 Futu provider：根据当前 SDK 的 `position_list_query()` 签名过滤参数，避免传入不支持的 `trd_market`。
- 继续修复 OpenD 跨网限制：将生产默认端口调整为 `11112`，并补充宿主机 `socat` 代理方案。
- 已提交并推送修复：`1a61678 Fix Futu position query compatibility`、`75d8baf Harden Futu OpenD compatibility`。

## 当前卡点

- 需要确认 GitHub Actions 最新部署完成。
- 需要在 ECS 上把 OpenD 改为监听 `127.0.0.1:11111`，再用 `socat` 在 Docker bridge 地址暴露 `11112` 给容器。
- OpenD 当前用命令行带密码启动，后续需要改成更安全的常驻方式，并建议用户更换已暴露过的富途登录密码。

## 下一步

- 将 ECS 上 OpenD 改为监听 `127.0.0.1:11111`，并启动 `socat` 代理到 `11112`。
- 确认容器内能连通 `host.docker.internal:11112`。
- 部署完成后在钉钉测试 `@机器人 我的持仓`。
- 如果持仓能返回，再做 OpenD systemd 常驻和密码安全处理。
