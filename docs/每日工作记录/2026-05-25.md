# 2026-05-25 核心记录

## 当前状态

- ECS 上 Futu OpenD 已启动，但直接监听 `0.0.0.0:11111` 会触发富途跨网交易连接限制。
- 钉钉 `我的持仓` 指令已能连到 OpenD，但持仓查询遇到 SDK 参数兼容问题。

## 今日完成

- 确认 OpenD 进程存在，且 `11111` 端口监听成功。
- 钉钉侧触发 `我的持仓`，错误从连接失败推进到 `position_list_query()` 参数不兼容。
- 修复 Futu provider：根据当前 SDK 的 `position_list_query()` 签名过滤参数，避免传入不支持的 `trd_market`。
- 明确 OpenD 正确连接方式：OpenD 监听 `127.0.0.1:11111`，宿主机用 `socat` 在 Docker bridge 地址暴露 `11112` 代理给容器。

## 当前卡点

- 需要部署兼容修复和 `11112` 代理配置后，再次测试 `@机器人 我的持仓`。
- OpenD 当前用命令行带密码启动，后续需要改成更安全的常驻方式，并建议用户更换已暴露过的富途登录密码。

## 下一步

- 提交并部署 Futu SDK 参数兼容修复。
- 将 ECS 上 OpenD 改为监听 `127.0.0.1:11111`，并启动 `socat` 代理到 `11112`。
- 部署完成后在钉钉测试 `@机器人 我的持仓`。
- 如果持仓能返回，再做 OpenD systemd 常驻和密码安全处理。
