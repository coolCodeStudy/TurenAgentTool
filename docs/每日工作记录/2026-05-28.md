# 2026-05-28 核心记录

## 当前状态

- InvestmentKnowledge 已迁移到阿里云新加坡 ECS 的主链路上。
- 新加坡 ECS 已确认可以连接 OpenAI，`持仓分析` 已返回 OpenAI 生成的组合复盘。
- 钉钉 Stream bot 已能接收指令，富途 OpenD 通过本机 `127.0.0.1:11111` 和 Docker bridge 代理 `11112` 被容器访问。
- 富途实时持仓查询与 OpenAI 持仓分析均已跑通。

## 今日完成

- 确认国内 ECS 访问 OpenAI 不通，判断迁移到新加坡 ECS 更适合继续 MVP。
- 在新加坡 ECS 上验证 `api.openai.com:443` 可达。
- 处理 Docker、部署、OpenAI 网络、云服务迁移和 OpenD 安装等基础设施问题。
- 下载并解压官方 Ubuntu OpenD 包，定位真实 `FutuOpenD` 可执行文件目录。
- 使用 simple systemd 方案启动 OpenD，并完成手机验证码验证。
- 确认 OpenD 监听 `127.0.0.1:11111`，`socat` 代理监听 `172.17.0.1:11112`。
- 从容器内验证 `host.docker.internal:11112` 可达。
- 钉钉触发 `持仓分析`，确认返回的是 OpenAI 版组合分析，不再是本地 fallback。

## 当前卡点

- 新加坡云端数据库是新库，当前个股知识匹配为 0，组合/策略心得也为 0。
- OpenD simple systemd 方案仍会在进程参数中暴露富途登录密码，后续应优化或在稳定后修改密码。
- 钉钉写入心得链路尚未正式打开，需要继续保持白名单，避免群消息污染知识库。

## 下一步

- 先补充组合/策略级心得，让 `持仓分析` 更贴合用户偏好。
- 打开并验证“仅用户本人可写入心得”的钉钉白名单链路。
- 补前十大持仓的股票画像和板块归属，提升组合分析质量。
- 稳定后考虑优化 OpenD 登录凭据安全，并确认是否停用国内 ECS 上的旧 bot。
