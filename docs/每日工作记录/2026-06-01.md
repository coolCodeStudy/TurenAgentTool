# 2026-06-01 核心记录

## 当前状态

- InvestmentKnowledge 已经从单纯钉钉查询工具，推进到“钉钉/Hermes + 云端 Codex worker + Codex App MCP”的协作架构讨论阶段。
- Codex worker 已完成 ChatGPT 设备授权登录，服务器能识别 `Logged in using ChatGPT`；但开发任务自动执行体验仍未闭环。
- 当前更明确的目标是减少人工 SSH/复制日志，让钉钉和 Codex App 都能通过同一套受控能力查看云端状态、日志和开发任务。

## 今日完成

- 修复并推送了多轮 Codex worker 相关问题：环境变量引号、worker 状态诊断、Postgres 连接地址、任务领取 SQL 参数类型、失败日志捕获、开发任务重试命令。
- 确认 Codex App 支持通过 MCP 接入外部工具；新的方向是做 InvestmentKnowledge Ops MCP，而不是继续让用户手工搬运 ECS 日志。
- 明确 Hermes 的定位：钉钉入口和消息网关；Ops API/MCP 作为底层能力层，可同时给 Hermes、钉钉和 Codex App 使用。

## 当前卡点

- Codex worker 虽然已登录，但 `codex exec` 执行链路仍需继续验证；现有“开发任务 #id”交互不符合用户希望的自然多轮协作体验。
- 云端日志、系统状态、worker 错误目前还没有统一的受控查询入口，导致排错仍依赖用户复制日志。
- 需要避免 worker 失败通知刷屏，错误通知要限流、聚合、脱敏。

## 下一步

- 优先实现 ECS Ops API：提供 `system_status`、`recent_errors`、`service_logs`、`coding_status` 等只读诊断能力。
- 基于 Ops API 做本地/远程 MCP 包装，让 Codex App 可以直接查询云端状态和日志。
- 让 Hermes/钉钉复用同一套 Ops API，再继续优化“自然语言开发会话”而不是显式任务编号交互。
