# 2026-05-21 核心记录

## 当前状态

- 云端 MVP 的钉钉主链路已打通。
- ECS 上 `dingtalk-stream-bot` 已能连接 PostgreSQL、连上 DingTalk Stream、接收群消息并回复 `帮助`。
- 钉钉写入保护已实现并部署：查询类指令可开放给群成员，写入类指令需要白名单。

## 今日完成

- 查看 ECS 上 `dingtalk-stream-bot` 日志，发现 `psycopg.OperationalError: failed to resolve host 'postgres': [Errno -8] Servname not supported for ai_socktype`。
- 判断根因是生产环境把 `POSTGRES_PASSWORD` 直接拼进 `DATABASE_URL`，特殊字符可能导致 PostgreSQL URL 被错误解析。
- 修复生产连接方式：生产容器改为传 `POSTGRES_HOST/PORT/USER/PASSWORD/DB`，应用端用 psycopg 结构化参数连接数据库。
- 钉钉主链路已打通：ECS bot 连上 DingTalk Stream，群消息能投递到 bot，并能回复 `帮助`。
- 增加钉钉 Stream 写入白名单：查询类指令可开放给群成员，写入/确认/记录心得类指令必须发送者命中 `DINGTALK_STREAM_WRITE_ALLOWED_SENDERS`。
- 提交并成功部署：
  - `05bdb16 Fix production Postgres connection config`
  - `ac7c89c Restrict DingTalk stream writes by sender`

## 当前卡点

- 如果后续要打开钉钉写入，需要先从日志里确认用户本人的 `sender_staff_id` 或 `sender_id`，再配置白名单。
- 需要测试 `@接接接的claw 怎么看海力士`，判断云端是否已有海力士数据。
- 云端数据库大概率还缺少本地海力士知识数据，需要准备最小数据迁移。

## 下一步

- 让用户在钉钉再发一次 `@接接接的claw 帮助`，从新日志里拿到 sender 标识：

```bash
cd /opt/investment-knowledge
docker compose -f docker-compose.prod.yml logs --tail=80 dingtalk-stream-bot
```

- 把用户本人的 `sender_staff_id` 或 `sender_id` 配置到 GitHub Secret `DINGTALK_STREAM_WRITE_ALLOWED_SENDERS`。
- 测试 `@接接接的claw 怎么看海力士`，如果未找到股票，就进入本地到云端的数据迁移。
