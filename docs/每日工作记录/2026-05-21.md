# 2026-05-21 核心记录

## 当前状态

- 云端 MVP 排查继续推进。
- 已定位钉钉 Stream bot 不响应的直接原因：bot 启动时连接 PostgreSQL 失败，尚未进入钉钉长连接阶段。

## 今日完成

- 查看 ECS 上 `dingtalk-stream-bot` 日志，发现 `psycopg.OperationalError: failed to resolve host 'postgres': [Errno -8] Servname not supported for ai_socktype`。
- 判断根因是生产环境把 `POSTGRES_PASSWORD` 直接拼进 `DATABASE_URL`，特殊字符可能导致 PostgreSQL URL 被错误解析。
- 修复生产连接方式：生产容器改为传 `POSTGRES_HOST/PORT/USER/PASSWORD/DB`，应用端用 psycopg 结构化参数连接数据库。

## 当前卡点

- 需要部署修复后的版本到 ECS。
- 部署后需要重新查看 bot 日志，确认是否出现 `DingTalk Stream bot starting`。
- 如果 bot 成功启动，再测试钉钉群里 `@接接接的claw 帮助` 和 `@接接接的claw 怎么看海力士`。

## 下一步

- 提交并推送本次修复，触发 GitHub Actions 自动部署。
- 部署完成后复查：

```bash
cd /opt/investment-knowledge
docker compose -f docker-compose.prod.yml ps
docker compose -f docker-compose.prod.yml logs --tail=200 dingtalk-stream-bot
```

