# 2026-05-27 核心记录

## 当前状态

- InvestmentKnowledge 云端钉钉链路已跑通到富途持仓查询入口。
- 富途实时持仓已在钉钉返回成功，证明云端链路可用。
- 当前优先级从“持仓复盘”调整为 OpenD 安全常驻，暂不做组合分析功能。
- 代码侧默认容器访问 `host.docker.internal:11112`，由 ECS 宿主机本机代理转发到 OpenD 的 `127.0.0.1:11111`。
- 最新部署 `75d8baf Harden Futu OpenD compatibility` 已在 GitHub Actions 成功完成。

## 今日完成

- 新建当天工作记录，保留当前恢复点。
- 确认最新 GitHub Actions 部署成功。
- 新增 `scripts/setup_futu_opend_proxy.sh`，用于在 ECS 上安装/启动 `socat` 代理，并检查 OpenD 本机端口可达。
- 更新富途持仓接入文档，加入脚本化操作方式。
- 根据 ECS 输出定位到根因：OpenD 当前仍监听 `0.0.0.0:11111`，代理 `172.17.0.1:11112` 已可达，但 OpenD 仍会按跨网交易连接处理。
- 加固代理脚本：如果发现 OpenD 监听 `0.0.0.0:11111`，直接失败并要求改为 `127.0.0.1`。
- 重启 OpenD 为 `127.0.0.1:11111` 后，钉钉 `我的持仓` 已成功返回 32 个实时持仓。
- 新增 `scripts/install_futu_opend_systemd.sh`，用于把 OpenD 和 `socat` 代理安装为 systemd 常驻服务，并避免明文密码出现在进程参数中。
- 用户执行 systemd 安装脚本时遇到 `Appdata.dat not found`；由于当前 OpenD 已能运行，说明该检查过于严格，已调整为 warning。

## 当前卡点

- 需要在 ECS 上执行 OpenD systemd 安装脚本。
- 安装后需要确认 `ps -ef` 不再显示富途登录密码，`11111/11112` 监听状态正常。
- 用户后续应考虑更换调试阶段暴露过的富途登录密码。

## 下一步

- 在 ECS 上执行 `bash scripts/install_futu_opend_systemd.sh --opend-dir ...`。
- 检查 `systemctl status futu-opend.service` 和 `systemctl status futu-opend-proxy.service`。
- 再次钉钉发送 `@机器人 我的持仓` 做回归验证。
- 暂不推进“持仓复盘”分析功能。
