# 2026-05-27 核心记录

## 当前状态

- InvestmentKnowledge 云端钉钉链路已跑通到富途持仓查询入口。
- 当前主要问题仍是 Futu OpenD 连接方式：容器直连宿主机 `0.0.0.0:11111` 会触发富途跨网交易连接限制。
- 代码侧已默认改为容器访问 `host.docker.internal:11112`，预期由 ECS 宿主机本机代理转发到 OpenD 的 `127.0.0.1:11111`。
- 最新部署 `75d8baf Harden Futu OpenD compatibility` 已在 GitHub Actions 成功完成。

## 今日完成

- 新建当天工作记录，保留当前恢复点。
- 确认最新 GitHub Actions 部署成功。
- 新增 `scripts/setup_futu_opend_proxy.sh`，用于在 ECS 上安装/启动 `socat` 代理，并检查 OpenD 本机端口可达。
- 更新富途持仓接入文档，加入脚本化操作方式。
- 根据 ECS 输出定位到根因：OpenD 当前仍监听 `0.0.0.0:11111`，代理 `172.17.0.1:11112` 已可达，但 OpenD 仍会按跨网交易连接处理。
- 加固代理脚本：如果发现 OpenD 监听 `0.0.0.0:11111`，直接失败并要求改为 `127.0.0.1`。

## 当前卡点

- 需要在 ECS 上重启 OpenD，使其只监听 `127.0.0.1:11111`。
- `socat` 已在 Docker bridge 地址监听 `11112`，容器内 `host.docker.internal:11112` 可达。
- OpenD 启动方式仍需后续改为更安全的常驻方案，避免命令行暴露登录信息。

## 下一步

- 停止当前 OpenD 进程，并用 `-api_ip=127.0.0.1` 重启。
- 在 ECS 上执行 `bash scripts/setup_futu_opend_proxy.sh` 或 `bash scripts/setup_futu_opend_proxy.sh --restart`。
- 从容器内测试 `host.docker.internal:11112` 是否可达。
- 钉钉发送 `@机器人 我的持仓` 做端到端验证。
