# 2026-05-27 核心记录

## 当前状态

- 国内阿里云 ECS 已跑通钉钉 Stream bot、Postgres、富途 OpenD 只读持仓查询。
- OpenD 最终采用 simple systemd 方案：OpenD 监听 `127.0.0.1:11111`，`socat` 代理监听 Docker bridge 的 `11112`。
- 钉钉 `我的持仓` 已能返回完整持仓明细，不再省略后续持仓。
- 钉钉 `持仓分析` / `今天仓位怎么看` / `组合体检` 已上线第一版，本地模板可用；OpenAI 在国内 ECS 因网络不可达暂时不能使用。
- 新加坡阿里云 ECS 已验证 `api.openai.com:443` 可达，但还未安装 Docker。
- 用户决定不迁移旧数据库数据，直接以新加坡机器作为后续主运行环境。

## 今日完成

- 修复并简化 OpenD 常驻方案，避免之前 XML / `Appdata.dat` / Python 兼容性问题阻塞。
- 通过钉钉验证富途实时持仓查询成功。
- 更新持仓展示逻辑，展示全部持仓。
- 新增港股新股查询分组：可申购、已截止待上市、已上市或其他。
- 新增 IPO 提醒机制：招股截止前一天 21:00 提醒，上市前一天 12:30 提醒暗盘。
- 修正文案：普通港股订单接口不能可靠反映个人 IPO 申购/中签记录，避免误报“未发现记录”。
- 新增持仓分析命令，基于富途实时持仓、仓位集中度、盈亏分布、知识库和组合/策略心得生成组合复盘。
- 优化 GitHub Actions quick/full deploy 分流，Python 和文档改动可走 quick deploy。
- 确认国内 ECS 无法连接 OpenAI，新加坡 ECS 可连接 OpenAI。
- 明确迁移策略：不迁旧数据，先在新加坡 ECS 安装 Docker、部署代码，再重跑 OpenD 脚本。

## 当前卡点

- 新加坡 ECS 还没有 Docker / Docker Compose。
- 新加坡 ECS 还未部署 InvestmentKnowledge。
- 新加坡 ECS 还未安装并登录 Futu OpenD。
- 同一个钉钉 Stream 机器人不适合长期新旧两台机器同时运行，最终需要停掉旧国内 bot，让新加坡 bot 接管。

## 下一步

- 在新加坡 ECS 安装 Docker 和 Docker Compose plugin。
- 将 GitHub Actions 的 `ECS_HOST` / `ECS_USERNAME` / `ECS_PASSWORD` 切到新加坡 ECS，并手动触发 full deploy。
- 在新加坡 ECS 上运行 `scripts/install_futu_opend_simple_systemd.sh`，重新登录富途 OpenD。
- 钉钉验证 `帮助`、`我的持仓`、`持仓分析`、`港股新股`。
- 确认新加坡 bot 稳定后，停止旧国内 ECS 的 `dingtalk-stream-bot`。
