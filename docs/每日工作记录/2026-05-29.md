# 2026-05-29 核心记录

## 当前状态

- 新加坡 ECS 链路已跑通，钉钉、OpenD、OpenAI 组合分析都具备可用基础。
- 组合分析已修正为多币种口径：未配置汇率时不再直接合计 HKD / USD，也不再输出跨币种 TopN 占比。
- GitHub Actions 部署在 scp 上传阶段遇到远端 `/tmp` 旧包权限问题；修复已本地提交，尚待推送。

## 今日完成

- 修复 `我的持仓` 展示：多币种时按币种汇总和币种内占比展示。
- 修复 `持仓分析` 上下文：增加币种汇总、币种内权重、按币种主要持仓和拖累项。
- 优化 OpenAI 组合分析 prompt：强制先说明数据口径，禁止在多币种未折算时输出误导性总市值和集中度。
- 保存 `docs/Agent工作模式演进计划.md`，记录从命令机器人演进到投资 Agent 运行时的方案。
- 修复部署 workflow：上传 release 包前清理 ECS `/tmp` 旧包，避免 permission denied。

## 当前卡点

- `2810de4 Clean stale deploy bundles before upload` 已在本地提交，但还未推送到 GitHub。
- 需要推送后等待 quick deploy 重新跑完，再在钉钉验证 `我的持仓` 和 `持仓分析` 的实际输出。
- 前十大持仓知识初始化和组合/策略级心得入库先暂停，等持仓币种口径稳定后再继续。

## 下一步

- 推送 `2810de4` 并确认 GitHub Actions 部署成功。
- 在钉钉测试 `我的持仓`，确认不再出现跨币种合计总市值。
- 在钉钉测试 `持仓分析`，确认输出包含数据口径，并按币种/市场分析。
- 若用户认可新版口径，再设计汇率/基准币种方案，随后恢复持仓知识初始化。
