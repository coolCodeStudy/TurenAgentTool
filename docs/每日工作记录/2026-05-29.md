# 2026-05-29 核心记录

## 当前状态

- 新加坡 ECS 链路已跑通，钉钉、OpenD、OpenAI 组合分析都具备可用基础。
- 组合分析已修正为多币种口径：未配置汇率时不再直接合计 HKD / USD，也不再输出跨币种 TopN 占比。
- 钉钉主动推送 webhook 已改走 GitHub Secret + full deploy；后续用 `系统状态` / `IPO提醒状态` 在钉钉内自检。
- 系统开始从固定命令机器人演进为受控 Intent Router：模型/规则负责理解自然语言，执行仍走白名单工具。

## 今日完成

- 修复 `我的持仓` 展示：多币种时按币种汇总和币种内占比展示。
- 修复 `持仓分析` 上下文：增加币种汇总、币种内权重、按币种主要持仓和拖累项。
- 优化 OpenAI 组合分析 prompt：强制先说明数据口径，禁止在多币种未折算时输出误导性总市值和集中度。
- 保存 `docs/Agent工作模式演进计划.md`，记录从命令机器人演进到投资 Agent 运行时的方案。
- 修复部署 workflow：上传 release 包前清理 ECS `/tmp` 旧包，避免 permission denied。
- 新增 `系统状态` / `IPO提醒状态` 指令，检查 PostgreSQL、OpenD、OpenAI、webhook、IPO 提醒循环和待提醒事项。
- 新增组合/策略心得命令：`记录组合心得`、`记录策略心得`、`提出组合候选心得`、`提出策略候选心得`。
- 新增自然语言 Intent Router 第一版：支持系统自检、IPO 状态、持仓查询/分析、交易复盘占位、自然语言候选心得沉淀。

## 当前卡点

- 最新功能提交已在本地完成，仍需推送后部署到云端验证。
- 交易复盘自然语言已能识别，但 Futu 历史成交、账户净资产快照、现金流和收益归因尚未接入。
- 前十大持仓知识初始化先暂停，等持仓币种口径、系统自检、交易复盘骨架更稳定后再继续。

## 下一步

- 推送并部署最新本地提交：系统状态指令、组合/策略心得命令、自然语言 Intent Router。
- 在钉钉测试：`系统状态`、`IPO提醒状态`、`我的持仓`、`持仓分析`。
- 下一项优先做 Futu 交易记录 / 月度收益复盘：历史成交、账户快照、现金流、区间收益归因。
- 将用户关于“低体力复盘、不过度盯盘、长期投资伴侣”的目标沉淀为候选策略心得，待用户确认。
