# 2026-05-30 核心记录

## 当前状态

- 钉钉入口已临时切回稳定的 `dingtalk-stream-bot`，Hermes Gateway 暂不接管钉钉消息。
- 云端 Codex worker 方案已接入代码侧：开发任务可进入 `coding_tasks`，worker 目标是领取任务、改代码、提交、推送分支并触发部署。
- 钉钉已能创建开发任务，后续需要确认 worker 是否能稳定领取并回传结果。

## 今日完成

- 修复 Hermes 无响应导致钉钉不可用的问题，先恢复旧 Stream bot 作为线上入口。
- 新增 Codex worker 自动化链路：任务领取、Codex 执行、commit/push、workflow deploy 触发。
- 新增钉钉侧开发任务详情查询：`查看开发任务 1` / `开发任务详情 1`。
- 新增 worker 完成或失败后的钉钉通知能力，减少手动查看 GitHub Actions/ECS 日志。
- 增强 deploy 日志，方便后续定位 Hermes/MCP/worker 链路问题。

## 当前卡点

- Hermes Gateway 接管钉钉后出现整体无响应，原因未彻底定位，暂时回滚到旧 bot。
- Codex worker 的真实端到端链路仍需验证：是否能在 ECS 上成功运行 Codex、推送分支、触发部署并通知钉钉。
- 钉钉体验还不是 Codex App 式实时长上下文，只是先补了任务详情和完成通知。

## 下一步

- 等最新部署完成后，在钉钉测试 `帮助`、`查看开发任务 1`、`创建开发任务 ...`。
- 运行/检查 `Codex Worker -> install`，确认 `investment-codex-worker.service` 正常。
- 用一个小开发任务验证完整闭环：钉钉创建任务 -> worker 改代码 -> push 分支 -> deploy -> 钉钉通知。
- Hermes Gateway 后续单独排查，不再阻塞主线迭代。
