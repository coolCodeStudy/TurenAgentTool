# 2026-05-24 核心记录

## 当前状态

- 项目主线仍停在云端 Futu OpenD 接入验证阶段。
- InvestmentKnowledge 云端 bot 已具备 `我的持仓` 查询入口。
- 当前需要把 ECS 上的 Futu OpenD 跑起来，才能真正读取实时持仓。

## 今日完成

- 自动保存了当天核心记录。
- 未观察到新的代码提交或新的排查结果。

## 当前卡点

- ECS 上 OpenD 尚未确认启动成功。
- 需要确认 OpenD 是否监听 `0.0.0.0:11111`，并能被 Docker 容器访问。
- 持仓查询目前依赖 OpenD 服务可用。

## 下一步

- 继续在 ECS 上启动 OpenD：

```bash
cd /opt/futu-opend/Futu_OpenD_10.6.6608_Centos7/Futu_OpenD_10.6.6608_Centos7
sudo chmod +x FutuOpenD
sudo ./FutuOpenD -login_account='你的富途账号' -login_pwd='你的富途登录密码' -api_ip=0.0.0.0 -api_port=11111 -lang=chs
```

- 另开 SSH 窗口检查：

```bash
ss -lntp | grep 11111
```

- 监听成功后，在钉钉测试 `@机器人 我的持仓`。

