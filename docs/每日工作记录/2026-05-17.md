# 2026-05-17 核心记录

## 当前状态

- 本地 MVP 已跑通：PostgreSQL/pgvector、股票画像、板块、知识项、用户心得、候选心得、OpenAI 分析、钉钉 Stream bot 都已接入。
- SK 海力士已作为第一只真实股票录入，并完成过一次“怎么看海力士”的分析闭环。
- GitHub Actions 到阿里云 ECS 的部署链路已跑通。
- ECS 上 `postgres` 和 `dingtalk-stream-bot` 容器已成功启动。
- 当前卡点：云端钉钉群里 `@接接接的claw` 暂时没有响应。

## 今日完成

- 明确普通钉钉 webhook 只能发消息，接收用户指令需要 DingTalk Stream bot。
- 将部署方式调整为 GitHub Actions 构建源码包和 Docker 镜像，再上传 ECS，避免 ECS 直接拉 GitHub/DockerHub 不稳定。
- 云端钉钉入口暂时只开放查询类指令，写入类命令先关闭。
- 新增每日核心记录机制，后续按日期保存到 `docs/每日工作记录/YYYY-MM-DD.md`。

## 当前卡点

- 需要确认 DingTalk Stream bot 是否真正连上钉钉长连接。
- 需要确认钉钉是否把群消息投递给 bot。
- 如果 bot 能收到消息，还需要确认是否存在 OpenAI、数据库或命令解析错误。

## 下一步

先看 ECS 容器日志：

```bash
cd /opt/investment-knowledge
docker compose -f docker-compose.prod.yml ps
docker compose -f docker-compose.prod.yml logs --tail=200 dingtalk-stream-bot
docker compose -f docker-compose.prod.yml logs --tail=80 postgres
```

判断逻辑：

- 如果 `帮助` 都不回，优先查 DingTalk Stream 连接或机器人配置。
- 如果 `帮助` 回但 `怎么看海力士` 不回，优先查云端数据库是否缺少本地海力士数据。
- 如果日志显示收到消息但处理失败，按报错修 command router、OpenAI 或数据库连接。
