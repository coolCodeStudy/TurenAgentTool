# 2026-05-31 核心记录

## 当前状态

- 钉钉主入口继续使用稳定的 `dingtalk-stream-bot`，Hermes Gateway 暂不接管消息。
- 云端 Codex worker 已进入认证与启动调试阶段，开发任务仍可通过钉钉创建和查看。
- 用户已完成 Codex 设备码登录授权，下一步需要重新安装/启动 worker 并确认心跳。

## 今日完成

- 修复 Codex worker 启动诊断：启动失败会写入 `worker_status`，钉钉可通过 `worker状态` 查询。
- 修复 worker 宿主机连接 PostgreSQL 的配置优先级，避免误用 Docker 内部 hostname `postgres`。
- 将 Codex worker 默认认证方式改为 ChatGPT/Codex 登录态，不再默认使用 OpenAI API key 计费。
- 新增 `Codex Worker -> login` 设备码登录模式，并强制刷新旧登录态。
- 为 Codex worker 启动失败通知增加限流，同一启动错误默认一小时最多推送一次钉钉。

## 当前卡点

- 需要确认 ECS 上新的 Codex 登录态是否已被 worker 正确识别。
- 需要重新运行 `Codex Worker -> install`，确认 worker 不再报 PostgreSQL hostname 或认证错误。
- 云端 worker 端到端闭环仍未完成验证：领取任务、修改代码、提交推送、本机部署、钉钉通知。

## 下一步

- 运行 `Codex Worker -> install`。
- 钉钉发送 `worker状态`，确认 worker 进入 `idle` 或 `running`。
- 用已有任务 `#1 帮我把本月收益展示格式优化一下` 验证云端 Codex worker 完整闭环。
- 若 worker 仍失败，优先看 `worker状态` 和 `Codex Worker -> status` 中最新错误。
