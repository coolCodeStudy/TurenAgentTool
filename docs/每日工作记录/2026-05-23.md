# 2026-05-23 核心记录

## 当前状态

- 云端钉钉主链路已打通。
- 富途持仓只读查询入口已部署，支持 `我的持仓` / `我的仓位` 等指令。
- 代码侧已准备好通过 ECS 上的 OpenD 读取实时持仓。

## 今日完成

- 自动保存了当天核心记录。
- 未观察到新的代码提交或新的排查结果。

## 当前卡点

- ECS 上仍需完成 Futu OpenD 启动、登录和 `11111` 端口监听。
- 钉钉写入开关仍保持关闭。
- 云端数据库大概率还缺少本地海力士知识数据。

## 下一步

- 在 ECS 上进入 OpenD 目录，确认 `FutuOpenD`、`FutuOpenD.xml`、`Appdata.dat` 存在。
- 前台启动 OpenD，并让它监听 `0.0.0.0:11111`。
- 用容器连通性测试确认 `host.docker.internal:11111` 可达。
- 钉钉测试 `@机器人 我的持仓`。

