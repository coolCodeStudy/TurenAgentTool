# 2026-06-06 核心记录

## 当前状态

- 主线继续转向业务功能：收益复盘、持仓知识初始化、组合图谱。
- 云端主链路仍是 `Codex App -> InvestmentKnowledge MCP /mcp -> ECS 内部 Ops API`。
- 当前代码库已有研究任务管线相关提交，但工作区还有未提交改动。

## 今日完成

- 新增 Codex-first research job pipeline，用于把持仓研究任务排队给研究 worker 处理。
- 新增组合/持仓研究回补命令，并降低批量回补规模，避免一次性任务过重。
- 部署流程开始安装 research agent worker 及其项目依赖。

## 当前卡点

- 工作区仍有未提交改动：`command_router.py`、`research/jobs.py`、`codex_task_worker.py`、`research_agent_worker.py`。
- 研究 worker 链路需要继续确认云端运行状态、任务消费、失败重试和结果回写。
- `/mcp` 访问保护仍是安全 TODO，暂未处理。

## 下一步

- 收敛并提交当前未完成改动，确认 research worker 能稳定领取和处理任务。
- 用前十大持仓跑一轮研究任务，验证草稿生成、review、入库前确认流程。
- 继续完善 `本月收益`，把估算展示升级为基于账户快照和汇率口径的月度复盘。
