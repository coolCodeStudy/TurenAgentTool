x-app-access-token: &app-access-token ${APP_ACCESS_TOKEN:-${COMMAND_API_TOKEN:-${WEEKLY_REVIEW_WEB_TOKEN:-}}}

services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${POSTGRES_HOST_PORT:-55432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-investment_kg}"]
      interval: 5s
      timeout: 3s
      retries: 20

  mcp:
    profiles: ["http"]
    image: investment-knowledge-app:${APP_IMAGE_TAG:-prod}
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./db:/app/db:ro
      - ./investment_knowledge_mcp:/app/investment_knowledge_mcp:ro
      - ./scripts:/app/scripts:ro
      - ./drafts:/opt/investment-knowledge/drafts
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
      MCP_TRANSPORT: ${MCP_TRANSPORT:-streamable-http}
      MCP_HOST: ${MCP_HOST:-0.0.0.0}
      MCP_PORT: ${MCP_PORT:-8000}
      MCP_PATH: ${MCP_PATH:-/mcp}
      FUTU_OPEND_HOST: ${FUTU_OPEND_HOST:-host.docker.internal}
      FUTU_OPEND_PORT: ${FUTU_OPEND_PORT:-11112}
      FUTU_OPEND_TELNET_HOST: ${FUTU_OPEND_TELNET_HOST:-host.docker.internal}
      FUTU_OPEND_TELNET_PORT: ${FUTU_OPEND_TELNET_PORT:-22222}
      FUTU_SECURITY_FIRM: ${FUTU_SECURITY_FIRM:-FUTUSECURITIES}
      FUTU_TRADE_MARKET: ${FUTU_TRADE_MARKET:-HK}
      FUTU_TRADE_ENV: ${FUTU_TRADE_ENV:-REAL}
      FUTU_ACCOUNT_ID: ${FUTU_ACCOUNT_ID:-0}
      FUTU_ACCOUNT_INDEX: ${FUTU_ACCOUNT_INDEX:-0}
      FUTU_POSITION_CACHE_SECONDS: ${FUTU_POSITION_CACHE_SECONDS:-20}
      FUTU_POSITION_REFRESH_CACHE: ${FUTU_POSITION_REFRESH_CACHE:-true}
      DINGTALK_SEND_WEBHOOK: ${DINGTALK_SEND_WEBHOOK:-}
      DINGTALK_SEND_SECRET: ${DINGTALK_SEND_SECRET:-}
      DINGTALK_IPO_REMINDERS_ENABLED: ${DINGTALK_IPO_REMINDERS_ENABLED:-true}
      DINGTALK_IPO_REMINDER_INTERVAL_SECONDS: ${DINGTALK_IPO_REMINDER_INTERVAL_SECONDS:-300}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.2}
      OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
      OPS_API_URL: ${MCP_OPS_API_URL:-http://host.docker.internal:8767}
      OPS_API_TOKEN: ${OPS_API_TOKEN:-}
      OPS_API_DEPLOY_TIMEOUT_SECONDS: ${OPS_API_DEPLOY_TIMEOUT_SECONDS:-600}
    ports:
      - "${MCP_BIND_HOST:-127.0.0.1}:${MCP_HOST_PORT:-8000}:${MCP_PORT:-8000}"
    command: ["sh", "-c", "python scripts/init_db.py && python -m investment_knowledge_mcp.server"]

  weekly-review-web:
    profiles: ["http"]
    image: investment-knowledge-app:${APP_IMAGE_TAG:-prod}
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./db:/app/db:ro
      - ./investment_knowledge_mcp:/app/investment_knowledge_mcp:ro
      - ./scripts:/app/scripts:ro
      - ./drafts:/opt/investment-knowledge/drafts
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
      WEEKLY_REVIEW_WEB_HOST: ${WEEKLY_REVIEW_WEB_HOST:-0.0.0.0}
      WEEKLY_REVIEW_WEB_PORT: ${WEEKLY_REVIEW_WEB_PORT:-8010}
      APP_ACCESS_TOKEN: *app-access-token
      COMMAND_API_TOKEN: *app-access-token
      WEEKLY_REVIEW_WEB_TOKEN: *app-access-token
      FUTU_OPEND_HOST: ${FUTU_OPEND_HOST:-host.docker.internal}
      FUTU_OPEND_PORT: ${FUTU_OPEND_PORT:-11112}
      FUTU_OPEND_TELNET_HOST: ${FUTU_OPEND_TELNET_HOST:-host.docker.internal}
      FUTU_OPEND_TELNET_PORT: ${FUTU_OPEND_TELNET_PORT:-22222}
      FUTU_SECURITY_FIRM: ${FUTU_SECURITY_FIRM:-FUTUSECURITIES}
      FUTU_TRADE_MARKET: ${FUTU_TRADE_MARKET:-HK}
      FUTU_TRADE_ENV: ${FUTU_TRADE_ENV:-REAL}
      FUTU_ACCOUNT_ID: ${FUTU_ACCOUNT_ID:-0}
      FUTU_ACCOUNT_INDEX: ${FUTU_ACCOUNT_INDEX:-0}
      FUTU_POSITION_CACHE_SECONDS: ${FUTU_POSITION_CACHE_SECONDS:-20}
      FUTU_POSITION_REFRESH_CACHE: ${FUTU_POSITION_REFRESH_CACHE:-true}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.2}
      OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
      OPS_API_URL: ${MCP_OPS_API_URL:-http://host.docker.internal:8767}
      OPS_API_TOKEN: ${OPS_API_TOKEN:-}
      OPS_API_DEPLOY_TIMEOUT_SECONDS: ${OPS_API_DEPLOY_TIMEOUT_SECONDS:-600}
      APP_RELEASE_SHA: ${APP_RELEASE_SHA:-}
    ports:
      - "${WEEKLY_REVIEW_WEB_HOST_PORT:-8010}:${WEEKLY_REVIEW_WEB_PORT:-8010}"
      - "${COMMAND_API_HOST_PORT:-8001}:${WEEKLY_REVIEW_WEB_PORT:-8010}"
    command: ["sh", "-c", "python scripts/init_db.py && python -m investment_knowledge_mcp.weekly_review_web"]

  dingtalk-api:
    profiles: ["http"]
    image: investment-knowledge-app:${APP_IMAGE_TAG:-prod}
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./db:/app/db:ro
      - ./investment_knowledge_mcp:/app/investment_knowledge_mcp:ro
      - ./scripts:/app/scripts:ro
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
      DINGTALK_API_HOST: ${DINGTALK_API_HOST:-0.0.0.0}
      DINGTALK_API_PORT: ${DINGTALK_API_PORT:-8002}
      DINGTALK_OUTGOING_SECRET: ${DINGTALK_OUTGOING_SECRET:-}
      DINGTALK_ALLOW_WRITE_COMMANDS: ${DINGTALK_ALLOW_WRITE_COMMANDS:-false}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.2}
      OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
      OPS_API_URL: ${MCP_OPS_API_URL:-http://host.docker.internal:8767}
      OPS_API_TOKEN: ${OPS_API_TOKEN:-}
      OPS_API_DEPLOY_TIMEOUT_SECONDS: ${OPS_API_DEPLOY_TIMEOUT_SECONDS:-600}
    ports:
      - "${DINGTALK_API_HOST_PORT:-8002}:${DINGTALK_API_PORT:-8002}"
    command: ["sh", "-c", "python scripts/init_db.py && python -m investment_knowledge_mcp.dingtalk_api"]

  dingtalk-stream-bot:
    profiles: ["stream"]
    image: investment-knowledge-app:${APP_IMAGE_TAG:-prod}
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./db:/app/db:ro
      - ./investment_knowledge_mcp:/app/investment_knowledge_mcp:ro
      - ./scripts:/app/scripts:ro
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
      DINGTALK_STREAM_CLIENT_ID: ${DINGTALK_STREAM_CLIENT_ID:?DINGTALK_STREAM_CLIENT_ID is required}
      DINGTALK_STREAM_CLIENT_SECRET: ${DINGTALK_STREAM_CLIENT_SECRET:?DINGTALK_STREAM_CLIENT_SECRET is required}
      DINGTALK_STREAM_ALLOW_WRITE: ${DINGTALK_STREAM_ALLOW_WRITE:-false}
      DINGTALK_STREAM_WRITE_ALLOWED_SENDERS: ${DINGTALK_STREAM_WRITE_ALLOWED_SENDERS:-}
      DINGTALK_COMMAND_TIMEOUT_SECONDS: ${DINGTALK_COMMAND_TIMEOUT_SECONDS:-25}
      DINGTALK_SEND_WEBHOOK: ${DINGTALK_SEND_WEBHOOK:-}
      DINGTALK_SEND_SECRET: ${DINGTALK_SEND_SECRET:-}
      DINGTALK_IPO_REMINDERS_ENABLED: ${DINGTALK_IPO_REMINDERS_ENABLED:-true}
      DINGTALK_IPO_REMINDER_INTERVAL_SECONDS: ${DINGTALK_IPO_REMINDER_INTERVAL_SECONDS:-300}
      FUTU_OPEND_HOST: ${FUTU_OPEND_HOST:-host.docker.internal}
      FUTU_OPEND_PORT: ${FUTU_OPEND_PORT:-11112}
      FUTU_OPEND_TELNET_HOST: ${FUTU_OPEND_TELNET_HOST:-host.docker.internal}
      FUTU_OPEND_TELNET_PORT: ${FUTU_OPEND_TELNET_PORT:-22222}
      FUTU_SECURITY_FIRM: ${FUTU_SECURITY_FIRM:-FUTUSECURITIES}
      FUTU_TRADE_MARKET: ${FUTU_TRADE_MARKET:-HK}
      FUTU_TRADE_ENV: ${FUTU_TRADE_ENV:-REAL}
      FUTU_ACCOUNT_ID: ${FUTU_ACCOUNT_ID:-0}
      FUTU_ACCOUNT_INDEX: ${FUTU_ACCOUNT_INDEX:-0}
      FUTU_POSITION_CACHE_SECONDS: ${FUTU_POSITION_CACHE_SECONDS:-20}
      FUTU_POSITION_REFRESH_CACHE: ${FUTU_POSITION_REFRESH_CACHE:-true}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.2}
      OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
      OPS_API_URL: ${MCP_OPS_API_URL:-http://host.docker.internal:8767}
      OPS_API_TOKEN: ${OPS_API_TOKEN:-}
      OPS_API_DEPLOY_TIMEOUT_SECONDS: ${OPS_API_DEPLOY_TIMEOUT_SECONDS:-600}
    command:
      - sh
      - -c
      - >
        python scripts/init_db.py &&
        if [ "$$DINGTALK_STREAM_ALLOW_WRITE" = "true" ]; then
          python scripts/dingtalk_stream_bot.py --allow-write;
        else
          python scripts/dingtalk_stream_bot.py;
        fi

  scheduler-host:
    image: investment-knowledge-app:${APP_IMAGE_TAG:-prod}
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./db:/app/db:ro
      - ./investment_knowledge_mcp:/app/investment_knowledge_mcp:ro
      - ./scripts:/app/scripts:ro
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-investment_kg}
      DINGTALK_SEND_WEBHOOK: ${DINGTALK_SEND_WEBHOOK:-}
      DINGTALK_SEND_SECRET: ${DINGTALK_SEND_SECRET:-}
      DINGTALK_IPO_REMINDERS_ENABLED: ${DINGTALK_IPO_REMINDERS_ENABLED:-true}
      DINGTALK_IPO_REMINDER_INTERVAL_SECONDS: ${DINGTALK_IPO_REMINDER_INTERVAL_SECONDS:-300}
      ACCOUNT_SNAPSHOT_SCHEDULER_ENABLED: ${ACCOUNT_SNAPSHOT_SCHEDULER_ENABLED:-true}
      ACCOUNT_SNAPSHOT_TIME: ${ACCOUNT_SNAPSHOT_TIME:-00:05}
      ACCOUNT_SNAPSHOT_INTERVAL_SECONDS: ${ACCOUNT_SNAPSHOT_INTERVAL_SECONDS:-300}
      FUTU_OPEND_HOST: ${FUTU_OPEND_HOST:-host.docker.internal}
      FUTU_OPEND_PORT: ${FUTU_OPEND_PORT:-11112}
      FUTU_OPEND_TELNET_HOST: ${FUTU_OPEND_TELNET_HOST:-host.docker.internal}
      FUTU_OPEND_TELNET_PORT: ${FUTU_OPEND_TELNET_PORT:-22222}
      FUTU_SECURITY_FIRM: ${FUTU_SECURITY_FIRM:-FUTUSECURITIES}
      FUTU_TRADE_MARKET: ${FUTU_TRADE_MARKET:-HK}
      FUTU_TRADE_ENV: ${FUTU_TRADE_ENV:-REAL}
      FUTU_ACCOUNT_ID: ${FUTU_ACCOUNT_ID:-0}
      FUTU_ACCOUNT_INDEX: ${FUTU_ACCOUNT_INDEX:-0}
      FUTU_POSITION_CACHE_SECONDS: ${FUTU_POSITION_CACHE_SECONDS:-20}
      FUTU_POSITION_REFRESH_CACHE: ${FUTU_POSITION_REFRESH_CACHE:-true}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.2}
      OPENAI_ANALYSIS_ENABLED: ${OPENAI_ANALYSIS_ENABLED:-true}
    healthcheck:
      test: ["CMD", "python", "-m", "investment_knowledge_mcp.scheduler_service", "--check-health"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s
    command:
      - sh
      - -c
      - python scripts/init_db.py && python -m investment_knowledge_mcp.scheduler_service

volumes:
  postgres_prod_data:
